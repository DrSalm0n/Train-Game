window_set_fullscreen(false);

// Set the application surface size to match the display
surface_resize(application_surface, 634, 360);

// Set the window size to match the display
window_set_size(634 * 3, 360 * 3 );

window_center();

